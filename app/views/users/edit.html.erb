<h2>User Info</h2>
<%= form_for(@user) do |f| %>
	<p>name</p>
	<%= f.text_field : %>
	<p>image</p>
	<%= attachment_image_tag@user,:profile_image,:fill,60,60, %>
	<p>introduction</p>
	<%= f.text_area : %>
	<%= f.submit 'Update User' %>

	<% end %>
